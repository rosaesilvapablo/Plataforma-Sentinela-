import { ModulePlaceholder } from "@/components/ModulePlaceholder";

export function PlantaoTab() {
  return (
    <ModulePlaceholder
      title="Plantão"
      description="Período (início/fim) + juiz responsável + servidor responsável + tipo (ordinário/extraordinário/recesso). Consulta por data. Integração com Calendário."
      next="A implementar — tarefa #21."
    />
  );
}
